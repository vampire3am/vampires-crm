import { CalendarDays, ChevronLeft, ChevronRight } from "lucide-react";
import { useEffect, useRef, useState } from "react";
const months=["January","February","March","April","May","June","July","August","September","October","November","December"];
const years=[2026,2027,2028];
export function MonthYearPicker({month,year,onChange,required}:{month:string;year:string;onChange:(month:string,year:string)=>void;required?:boolean}){const[open,setOpen]=useState(false),[view,setView]=useState<"year"|"month">("year");const root=useRef<HTMLDivElement>(null);useEffect(()=>{const close=(event:MouseEvent)=>{if(!root.current?.contains(event.target as Node))setOpen(false)};document.addEventListener("mousedown",close);return()=>document.removeEventListener("mousedown",close)},[]);const selected=month&&year;return <div className="dob-picker intake-picker" ref={root}><button type="button" className={`dob-trigger ${selected?"selected":""}`} onClick={()=>setOpen(value=>!value)} aria-expanded={open}><span>{selected?`${month} ${year}`:"Choose intake month and year"}</span><CalendarDays size={18}/></button>{open&&<div className="dob-popover intake-popover"><header><button type="button" aria-label="Select year" onClick={()=>setView("year")}><ChevronLeft/></button><strong>{view==="year"?"Select intake year":year}</strong><button type="button" aria-label="Select month" onClick={()=>year&&setView("month")}><ChevronRight/></button></header>{view==="year"?<div className="intake-year-grid">{years.map(value=><button type="button" className={year===String(value)?"active":""} key={value} onClick={()=>{onChange(month,String(value));setView("month")}}>{value}</button>)}</div>:<div className="dob-month-grid">{months.map(value=><button type="button" className={month===value?"active":""} key={value} onClick={()=>{onChange(value,year);setOpen(false)}}>{value.slice(0,3)}</button>)}</div>}<footer><button type="button" onClick={()=>{onChange("","");setView("year")}}>Clear</button>{required&&<span>Month and year required</span>}</footer></div>}</div>}
